name: Personal DNS Server
on:
  workflow_dispatch:

jobs:
  dns-server:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y dnsmasq curl

    - name: Setup DNS Server
      run: |
        # توقف سرویس پیش فرض
        sudo systemctl stop systemd-resolved || true
        sudo systemctl disable systemd-resolved || true

        # ایجاد پیکربندی جدید
        sudo tee /etc/dnsmasq.conf > /dev/null << EOF
        interface=*
        listen-address=127.0.0.1
        bind-interfaces
        server=8.8.8.8
        server=1.1.1.1
        address=/steam.com/104.112.119.210
        address=/epicgames.com/104.16.109.18
        no-hosts
        no-resolv
        cache-size=1000
        EOF

        # راه اندازی مجدد سرویس
        sudo systemctl restart dnsmasq
        sudo systemctl enable dnsmasq
        
        # بررسی وضعیت سرویس
        sleep 3
        sudo systemctl status dnsmasq --no-pager
        echo "✅ DNS Server configured successfully!"

    - name: Verify DNS is working
      run: |
        # تست DNS سرور
        nslookup steam.com 127.0.0.1
        nslookup epicgames.com 127.0.0.1
        echo "✅ DNS queries are working!"

    - name: Setup Ngrok Tunnel
      run: |
        # دانلود و تنظیم Ngrok
        wget -q https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
        tar -xzf ngrok-v3-stable-linux-amd64.tgz
        chmod +x ngrok
        
        ./ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
        
        # راه اندازی تونل برای پورت 53
        ./ngrok tcp 53 --log=stdout > ngrok.log &
        sleep 15
        
        # دریافت اطلاعات تونل بدون jq
        TUNNEL_INFO=$(curl -s http://localhost:4040/api/tunnels || echo "")
        if [ -n "$TUNNEL_INFO" ]; then
            # استفاده از grep و sed برای استخراج اطلاعات
            TUNNEL_URL=$(echo "$TUNNEL_INFO" | grep -o '"public_url":"[^"]*' | cut -d'"' -f4 | head -1)
            if [ -n "$TUNNEL_URL" ]; then
                DNS_IP=$(echo "$TUNNEL_URL" | cut -d':' -f2 | sed 's#//##')
                DNS_PORT=$(echo "$TUNNEL_URL" | cut -d':' -f3)
                echo "🌐 DNS Tunnel Established!"
                echo "🔢 IP Address: $DNS_IP"
                echo "🔌 Port: $DNS_PORT"
                echo "DNS_IP=$DNS_IP" >> $GITHUB_ENV
                echo "DNS_PORT=$DNS_PORT" >> $GITHUB_ENV
            else
                echo "⚠️ Could not parse tunnel information"
                echo "Trying alternative method..."
                
                # روش جایگزین برای استخراج اطلاعات
                ALTERNATIVE_IP=$(curl -s http://localhost:4040/api/tunnels | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | head -1)
                if [ -n "$ALTERNATIVE_IP" ]; then
                    echo "🌐 Found IP: $ALTERNATIVE_IP"
                    echo "🔌 Default Port: 53"
                    echo "DNS_IP=$ALTERNATIVE_IP" >> $GITHUB_ENV
                    echo "DNS_PORT=53" >> $GITHUB_ENV
                else
                    echo "❌ Failed to get tunnel information"
                    echo "Ngrok log:"
                    cat ngrok.log || echo "No ngrok log found"
                fi
            fi
        else
            echo "⚠️ Could not get tunnel information"
            echo "Checking ngrok process..."
            ps aux | grep ngrok || echo "No ngrok process found"
        fi

    - name: Display Connection Info
      run: |
        echo "=========================================="
        echo "✅ PERSONAL DNS SERVER IS READY!"
        echo "=========================================="
        if [ -n "${{ env.DNS_IP }}" ]; then
            echo "🔢 DNS IP: ${{ env.DNS_IP }}"
            echo "🔌 DNS Port: ${{ env.DNS_PORT }}"
            echo "📱 Use this in your DNS changer app:"
            echo "Primary DNS: ${{ env.DNS_IP }}"
            echo "=========================================="
            echo "🎯 Test with: nslookup steam.com ${{ env.DNS_IP }}"
        else
            echo "⚠️ Could not get DNS IP. Check ngrok connection."
            echo "=========================================="
        fi

    - name: Keep alive
      run: |
        echo "🟢 DNS Server is running..."
        echo "⏰ Press Ctrl+C in log to stop the workflow"
        echo "📊 Testing connection every 30 seconds:"
        
        # نگهداری اجرا با تست دوره‌ای
        while true; do
            if [ -n "${{ env.DNS_IP }}" ]; then
                # تست اتصال DNS
                if nslookup steam.com ${{ env.DNS_IP }} >/dev/null 2>&1; then
                    echo "✅ Connection working at $(date +'%H:%M:%S')"
                else
                    echo "❌ Connection failed at $(date +'%H:%M:%S')"
                fi
            fi
            sleep 30
        done
