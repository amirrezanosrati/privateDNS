name: Personal DNS Server
on:
  workflow_dispatch:

jobs:
  dns-server:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y dnsmasq curl

    - name: Setup DNS Server
      run: |
        # توقف سرویس‌های conflicting
        sudo systemctl stop systemd-resolved || true
        sudo systemctl disable systemd-resolved || true
        
        # آزاد کردن پورت 53
        sudo fuser -k 53/udp || true
        sudo fuser -k 53/tcp || true

        # ایجاد پیکربندی dnsmasq
        sudo tee /etc/dnsmasq.conf > /dev/null << EOF
        interface=*
        listen-address=0.0.0.0
        bind-interfaces
        server=8.8.8.8
        server=1.1.1.1
        address=/steam.com/104.112.119.210
        address=/epicgames.com/104.16.109.18
        no-hosts
        no-resolv
        cache-size=1000
        EOF

        # راه اندازی dnsmasq
        sudo systemctl restart dnsmasq
        sleep 3
        
        # تست DNS
        echo "Testing DNS server..."
        nslookup steam.com 127.0.0.1 && echo "✅ DNS is working!"

    - name: Setup Ngrok (Simple Method)
      run: |
        # دانلود ngrok
        wget -q https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
        tar -xzf ngrok-v3-stable-linux-amd64.tgz
        chmod +x ngrok
        
        # تنظیم auth token
        ./ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
        
        # راه اندازی تونل در پس‌زمینه
        ./ngrok tcp 53 --log=stdout > ngrok.log 2>&1 &
        NGORK_PID=$!
        echo $NGORK_PID > ngrok.pid
        
        # منتظر ماندن برای راه اندازی تونل
        echo "Waiting for ngrok to start..."
        sleep 20
        
        # بررسی وضعیت ngrok
        if ps -p $NGORK_PID > /dev/null; then
            echo "✅ Ngrok is running successfully!"
        else
            echo "❌ Ngrok failed to start"
            cat ngrok.log
            exit 1
        fi

    - name: Get Connection Info
      run: |
        # دریافت اطلاعات تونل به صورت مستقیم
        echo "Getting tunnel information..."
        sleep 10
        
        # روش ساده برای دریافت آدرس
        TUNNEL_INFO=$(curl -s http://localhost:4040/api/tunnels || echo "")
        
        if [ -n "$TUNNEL_INFO" ]; then
            # استفاده از روش ساده برای استخراج
            TUNNEL_URL=$(echo "$TUNNEL_INFO" | grep -o 'tcp://[^"]*' | head -1)
            if [ -n "$TUNNEL_URL" ]; then
                DNS_IP=$(echo "$TUNNEL_URL" | cut -d':' -f2 | sed 's#//##')
                DNS_PORT=$(echo "$TUNNEL_URL" | cut -d':' -f3)
                echo "🌐 TUNNEL_URL: $TUNNEL_URL"
                echo "🔢 DNS_IP: $DNS_IP"
                echo "🔌 DNS_PORT: $DNS_PORT"
                echo "DNS_IP=$DNS_IP" >> $GITHUB_ENV
                echo "DNS_PORT=$DNS_PORT" >> $GITHUB_ENV
            else
                # روش جایگزین: استفاده از log file
                echo "Trying alternative method from log file..."
                LOG_IP=$(grep -o 'url=[^ ]*' ngrok.log | grep -o 'tcp://[^ ]*' | head -1 | cut -d':' -f2 | sed 's#//##')
                if [ -n "$LOG_IP" ]; then
                    echo "🌐 Found IP from log: $LOG_IP"
                    echo "DNS_IP=$LOG_IP" >> $GITHUB_ENV
                    echo "DNS_PORT=53" >> $GITHUB_ENV
                else
                    echo "⚠️ Using default ngrok domain"
                    echo "DNS_IP=0.tcp.ngrok.io" >> $GITHUB_ENV
                    echo "DNS_PORT=53" >> $GITHUB_ENV
                fi
            fi
        else
            echo "⚠️ Using fallback ngrok address"
            echo "DNS_IP=0.tcp.ngrok.io" >> $GITHUB_ENV
            echo "DNS_PORT=53" >> $GITHUB_ENV
        fi

    - name: Display Final Info
      run: |
        echo "=========================================="
        echo "🎯 YOUR PERSONAL DNS SERVER IS READY!"
        echo "=========================================="
        echo "🔢 DNS IP: ${{ env.DNS_IP }}"
        echo "🔌 DNS Port: ${{ env.DNS_PORT }}"
        echo ""
        echo "📱 در اپلیکیشن DNS Changer گوشی:"
        echo "Primary DNS: ${{ env.DNS_IP }}"
        echo "Port: ${{ env.DNS_PORT }}"
        echo ""
        echo "🔧 Test connection:"
        echo "nslookup steam.com ${{ env.DNS_IP }}"
        echo "=========================================="

    - name: Keep alive with health checks
      run: |
        echo "🟢 DNS Server is running..."
        echo "💡 Press the 'Cancel workflow' button to stop"
        echo ""
        
        COUNTER=0
        while true; do
            COUNTER=$((COUNTER + 1))
            echo "⏰ Running for $((COUNTER * 30)) seconds..."
            
            # تست سلامت
            if nslookup steam.com 127.0.0.1 >/dev/null 2>&1; then
                echo "✅ DNS: Healthy"
            else
                echo "❌ DNS: Not responding"
            fi
            
            # بررسی ngrok
            if [ -f "ngrok.pid" ] && ps -p $(cat ngrok.pid) >/dev/null; then
                echo "✅ Ngrok: Running"
            else
                echo "❌ Ngrok: Stopped"
            fi
            
            sleep 30
        done
